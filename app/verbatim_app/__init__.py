"""Verbatim, the local web app over a Verbatim instance.

The disk is the database: this package reads and writes the same markdown
files the skills do, in the format references/instance.md specifies, and
holds no state of its own.
"""

__version__ = "2.1.0"
